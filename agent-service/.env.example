# AgentX agent-service — zero-dependency Python runtime
#
# Runs the existing `backend` package (LangGraph orchestrator, 10 agents, RAG,
# SQLite seed) behind the stdlib http.server. No FastAPI/uvicorn/new deps.

AGENT_SERVICE_PORT=8100
